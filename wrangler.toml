name = "grades-viewer"
compatibility_date = "2026-09-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "./dist"

# D1 binding (替换 database_id 为真实值)
[[d1_databases]]
binding = "DB"
database_name = "grades-db"
database_id = "REPLACE_ME_AFTER_D1_CREATE"

[vars]
PUBLIC_VERSION = "0.1"
