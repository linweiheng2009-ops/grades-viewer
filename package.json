{
  "name": "grades-viewer",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/postbuild.mjs",
    "preview": "astro preview",
    "deploy": "node scripts/predeploy.mjs && wrangler deploy && node scripts/postbuild.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.2",
    "astro": "^7.3.3",
    "d3-shape": "^3.2.0",
    "papaparse": "^5.7.0",
    "wrangler": "^4.135.0"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@types/d3-shape": "^3.2.0",
    "@types/papaparse": "^5.5.2"
  }
}
